<Page
    x:Class="Markdown.CreateMarkdown"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="using:Syncfusion.DemosCommon.WinUI"
    xmlns:enumextension="using:Syncfusion.DemosCommon.WinUI"
    xmlns:core="using:Syncfusion.UI.Xaml.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Markdown"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"    
    Background="Transparent"
    mc:Ignorable="d">
    <StackPanel>
        <common:DemoLayout 
                HeaderText="This sample demonstrates how to create a Markdown document with various elements such as headings, text, images, lists, tables, hyperlinks, and code blocks using .NET Markdown library."
                CSharpSource="{common:ConditionalXAML Individual=Syncfusion.MarkdownDemos.WinUI/Assets/Markdown/CreateMarkdown_cs.txt,
                                                                 Common=Markdown/Assets/Markdown/CreateMarkdown_cs.txt}">
            <common:DemoLayout.Example>
                <Grid x:Name="GettingStarted" Margin="5,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="description" Grid.Row="3" Grid.ColumnSpan="4" TextWrapping="Wrap" Text="Click the &quot;Generate Markdown&quot; button to view the generated Markdown file. Please note that a Markdown-compatible viewer is required to open and view the Markdown document."/>
                    <TextBlock x:Name="text2" Grid.Row="4" Grid.ColumnSpan="4"/>
                    <Button x:Name="button1" Grid.Row="12" Grid.ColumnSpan="1" Content="Generate Markdown" HorizontalAlignment="Left" Click="Button_Click" Height="Auto" Width="150" Padding="10,10,10,10" Margin="0,10" />
                </Grid>
            </common:DemoLayout.Example>
        </common:DemoLayout>
    </StackPanel>
</Page>
